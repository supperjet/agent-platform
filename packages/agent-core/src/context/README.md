# Context

Owns per-turn context assembly and context budget decisions.

This directory is intentionally created during Harness phase 0 so new context-related code has a stable home instead of growing inside `runtime/`.
