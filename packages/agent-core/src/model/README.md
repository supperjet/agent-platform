# Model

Owns model references, model catalog lookup, provider gateway behavior, and auth/stream adapter concepts.

Provider-specific code should move here instead of living under ad hoc runtime modules.
